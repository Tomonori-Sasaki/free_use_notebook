<%= form_with(model: material, local: true) do |form| %>
  <% if material.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(material.errors.count, "error") %> prohibited this material from being saved:</h2>

      <ul>
      <% material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :material_name, class: "form-control" %>
  </div><br>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :material_content, class: "form-control", style: "height: 200px;" %>
  </div><br>

  <div class="actions" style="text-align: center;">
    <button class="button8" type="submit">Submit</button>
  </div><br>
<% end %>
