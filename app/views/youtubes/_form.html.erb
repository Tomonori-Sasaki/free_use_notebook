<%= form_with(model: youtube, local: true) do |form| %>
  <% if youtube.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(youtube.errors.count, "error") %> prohibited this youtube from being saved:</h2>

      <ul>
      <% youtube.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rank %>
    <%= form.number_field :rank, id: :youtube_name, class: "form-control" %>
  </div><br>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :youtube_name, class: "form-control" %>
  </div><br>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :youtube_content, class: "form-control", style: "height: 200px;" %>
  </div><br>


  <div class="actions" style="text-align: center;">
    <button class="button8" type="submit">Submit</button>
  </div><br>
<% end %>
